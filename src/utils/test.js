import { generarAlpha } from "./utils";

const cifrarAlpha =  () => {
    let alpha = generarAlpha();
    console.log(alpha);
}

const decifrarAlpha = () => {

}

export { cifrarAlpha, decifrarAlpha }